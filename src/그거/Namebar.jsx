import './menu.css';

function namebar({name}) {

    return (
        <div>
            <nav>
                <h1>{name}</h1>
            </nav>
        </div>

    );
}

export default namebar;
